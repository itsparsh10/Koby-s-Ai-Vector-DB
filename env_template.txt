# Copy this file to .env and update the values

# Django Settings
DJANGO_SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOW_ALL_ORIGINS=True
CORS_ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://localhost:3000
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_SSL_REDIRECT=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
SECURE_HSTS_PRELOAD=False

# Supabase Settings (recommended)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-or-publishable-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_PDF_BUCKET=pdfs
# Run supabase_schema.sql in the Supabase SQL editor to create app_users, user_auth_events, user_search_logs.

# Gemini AI Settings (optional)
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash-lite

# Production database (Render / Railway / Fly). Leave unset for local SQLite.
# DATABASE_URL=postgres://user:pass@host:5432/dbname
# DATABASE_SSL_REQUIRE=true

# Trust X-Forwarded-Host from reverse proxies (Render, Railway, Vercel edge)
USE_X_FORWARDED_HOST=true

# PDF Processing Settings
PDF_DIRECTORY=pdfs
INDEX_PATH=indexes/faiss_index.bin
METADATA_PATH=indexes/metadata.json
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
EMBED_MODEL_NAME=all-MiniLM-L6-v2

# Search Settings
MAX_SEARCH_RESULTS=5
SIMILARITY_THRESHOLD=0.3

# Render.com (set in dashboard; RENDER_* are injected automatically)
# DATABASE_URL=<Render Postgres Internal Database URL>
# See DEPLOY_RENDER.txt for full checklist.

# --- Deployment notes ---
# Full stack (Django + FAISS + sentence-transformers) is too large for Vercel serverless.
# Deploy the app with Docker (see Dockerfile) to Render, Railway, Fly.io, or Google Cloud Run.
# Optional: create a separate Vercel project using vercel.json to reverse-proxy your Django URL
# (replace REPLACE_WITH_YOUR_DJANGO_URL). Session cookies work best when users hit the Django
# URL directly or you use one custom domain pointed at that service.
